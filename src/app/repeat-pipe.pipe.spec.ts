import { RepeatPipePipe } from './repeat-pipe.pipe';

describe('RepeatPipePipe', () => {
  it('create an instance', () => {
    const pipe = new RepeatPipePipe();
    expect(pipe).toBeTruthy();
  });
});
